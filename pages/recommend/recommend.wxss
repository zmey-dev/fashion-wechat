/* pages/recommend/recommend.wxss */
.page-container {
  position: relative;
  width: 100vw;
  height: 100%;
  background-color: #000;
  overflow: hidden;
}

.post-viewer-container {
  position: relative;
  width: 100vw;
  height: 100%;
  background-color: #000;
  overflow: hidden;
}

/* Teacher layout - no tab bar */
.post-viewer-container.teacher-layout {
  height: calc(100vh - 120rpx);
}

/* Loading container - only show for initial load */
.loading-container {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: rgba(0, 0, 0, 0.8);
  z-index: 9999;
  width: 100vw;
  height: 100vh;
}

.loading-spinner {
  width: 80rpx;
  height: 80rpx;
  border: 8rpx solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  border-top-color: #fff;
  animation: spin 1s linear infinite;
}

.media-player {
  width: 100%;
  height: 100%;
  background-color: #000;
}

@keyframes spin {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}

.loading-text {
  display: none;
}

.error-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: rgba(0, 0, 0, 0.9);
  z-index: 999;
  padding: 40rpx;
}

.error-icon {
  font-size: 120rpx;
  margin-bottom: 40rpx;
}

.error-text {
  color: #fff;
  font-size: 32rpx;
  text-align: center;
  margin-bottom: 40rpx;
}

.retry-btn {
  padding: 20rpx 60rpx;
  background-color: #3b82f6;
  color: #fff;
  font-size: 28rpx;
  border-radius: 50rpx;
  border: none;
}

/* Empty State */
.empty-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background-color: rgba(0, 0, 0, 0.95);
  z-index: 999;
  padding: 40rpx;
}

.empty-icon {
  font-size: 120rpx;
  margin-bottom: 40rpx;
}

.empty-text {
  color: #fff;
  font-size: 32rpx;
  text-align: center;
  margin-bottom: 40rpx;
}

.login-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: rgba(0, 0, 0, 0.1);
  z-index: 1000;
}

/* Swiper styles for infinite scroll */
.posts-swiper {
  width: 100% !important;
  height: 100% !important;
  position: relative !important;
}

.swiper-item {
  width: 100% !important;
  height: 100% !important;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
