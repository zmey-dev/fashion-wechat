/* comment-tree.wxss */
.comment-tree-container {
  height: 100%;
  display: flex;
  flex-direction: column;
  background-color: rgba(0, 0, 0, 0.4);
  backdrop-filter: blur(8px);
  position: relative;
}

/* Comments List */
.comments-list {
  flex: 1;
  box-sizing: border-box;
  overflow-y: auto;
  padding: 16rpx 16rpx 240rpx 16rpx; /* Added bottom padding to make room for comment input */
}

.no-comments {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 200rpx;
}

.no-comments-text {
  color: #9ca3af;
  font-size: 28rpx;
  font-style: italic;
}

.comments-container {
  padding-bottom: 32rpx;
}

/* Comment Input Section - Fixed at bottom */
.comment-input-section {
  position: absolute;
  display: flex;
  flex-direction: column;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(0, 0, 0, 0.6);
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  padding: 16rpx;
  z-index: 10;
}

.reply-header {
  position: relative;
  padding: 12rpx 16rpx;
  background-color: rgba(0, 0, 0, 0.3);
  border-top-left-radius: 24rpx;
  border-top-right-radius: 24rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.3);
  margin-bottom: 2rpx;
}

.reply-close-btn {
  position: absolute;
  top: 28rpx;
  right: 28rpx;
  width: 28rpx;
  height: 28rpx;
  background: none;
  border: none;
  padding: 0;
  margin-left: auto;
}

.close-icon {
  width: 32rpx;
  height: 32rpx;
}

.reply-info {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
}

.reply-avatar-container {
  width: 48rpx;
  height: 48rpx;
  margin-right: 16rpx;
}

.reply-avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.reply-username {
  font-size: 28rpx;
  color: #e5e7eb;
  font-weight: 500;
  margin-right: 8rpx;
}

.reply-preview {
  font-size: 26rpx;
  color: #9ca3af;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 400rpx;
}

.reply-image-preview {
  width: 32rpx;
  height: 32rpx;
  border-radius: 8rpx;
  margin-left: 8rpx;
}

/* Comment Input Container */
.comment-input-container {
  display: flex;
  align-items: center;
  background-color: rgba(0, 0, 0, 0.3);
  border-radius: 24rpx;
  padding: 8rpx;
}

.comment-textarea {
  flex: 1;
  min-height: 72rpx;
  max-height: 360rpx;
  padding: 16rpx;
  background-color: transparent;
  color: #e5e7eb;
  font-size: 28rpx;
  border: none;
}

.comment-textarea.has-reply {
  border-top-left-radius: 0;
  border-top-right-radius: 0;
}

.comment-textarea::placeholder {
  color: #9ca3af;
  font-size: 26rpx;
}

.input-actions {
  width: 160rpx;
  display: flex;
  padding-top: auto;
  margin-top: auto;
  align-items: center;
}

.input-action-btn {
  background: none;
  border: none;
  padding: 8rpx;
  margin-left: 4rpx;
  width: 64rpx;
  height: 64rpx;
  display: flex;
  justify-content: center;
  align-items: center;
}

.action-icon {
  width: 40rpx;
  height: 40rpx;
}

/* Emoji Picker */
.emoji-picker {
  padding: 16rpx;
  background-color: rgba(0, 0, 0, 0.5);
  border-radius: 16rpx;
  margin-top: 8rpx;
  max-height: 240rpx;
  overflow-y: auto;
}

.emoji-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  justify-content: space-around;
}

.emoji-item {
  font-size: 48rpx;
  padding: 8rpx;
  border-radius: 8rpx;
  transition: all 0.2s ease;
}

.emoji-item:active {
  background-color: rgba(255, 255, 255, 0.1);
  transform: scale(1.2);
}

/* Comment Tree Item Styles */
.comment-tree-item {
  margin-bottom: 16rpx;
}

.reply-toggle-container {
  margin-left: 80rpx;
  margin-bottom: 16rpx;
}

.reply-toggle-btn {
  display: flex;
  align-items: center;
  background: none;
  border: none;
  color: #9ca3af;
  font-size: 24rpx;
  padding: 8rpx 0;
  transition: color 0.2s ease;
}

.reply-toggle-btn:active {
  color: #e5e7eb;
}

.toggle-icon {
  width: 28rpx;
  height: 28rpx;
  margin-right: 8rpx;
}

.toggle-text {
  font-size: 24rpx;
}

.replies-container {
  margin-left: 40rpx;
  padding-left: 24rpx;
  border-left: 2rpx solid rgba(156, 163, 175, 0.3);
  transition: all 0.3s ease;
  overflow: hidden;
}

.replies-container.expanded {
  max-height: 4000rpx;
  opacity: 1;
}

.replies-container.collapsed {
  max-height: 0;
  opacity: 0;
}